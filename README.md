# ray-tracer-redux
Revisiting my project from Computer Graphics and building a modern ray tracer.

# How to run
1. `g++ -0 main main.cc` to compile to `main` executable.
2. `./main` to run executable.
3.  `./main > output_image.ppm` to run and pipe to viewable image file.